package com.fixbridge.propertypassport;

/**
 * Property Passport documents and systems are served via
 * {@code com.fixbridge.property} (documents, property health fields on entities).
 * Expand this package when Passport-specific aggregates are extracted.
 */
public final class PropertyPassportPackage {
  private PropertyPassportPackage() {}
}
